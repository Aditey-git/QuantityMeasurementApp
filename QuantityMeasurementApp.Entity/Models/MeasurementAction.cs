namespace QuantityAppModel
{
    // Enum mapping user choices to measurement operations
    public enum MeasurementAction
    {
        Compare = 1,
        Add = 2,
        Subtract = 3,
        Divide = 4
    }
}
